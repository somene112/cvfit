import LoginForm from '@/components/login/LoginForm';

export const metadata = {
  title: 'Sign In — AI CV Fit Analyzer',
  description: 'Sign in to your AI CV Fit Analyzer account to analyze resumes and match skills.',
};

export default function LoginPage() {
  return <LoginForm />;
}
