For the Library Management System, I designed a database schema with tables for books, members, and loans. I implemented relationships using foreign keys and created indexes on frequently queried columns. The CRUD operations were handled through Python functions that validated input data and handled errors gracefully. For the quiz application, I used Flask to create REST endpoints for fetching questions, submitting answers, and calculating scores. I stored quiz data in SQLite and implemented session management for tracking user progress.
