"""Phase 7A billing service package (plan config and helpers).

No provider API calls live here. This package owns the authoritative credit-pack
configuration so amounts are always determined server-side, never by the client.
"""
